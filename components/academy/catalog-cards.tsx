import Link from 'next/link'
import { ArrowRight, Clock3, FileCheck2, Layers3 } from 'lucide-react'
import { NETWORKS, type LearningMission, type LearningPath, type LearningSkill } from '@/lib/academy-catalog'

export function MissionCard({ mission }: { mission:LearningMission }) { const sector=NETWORKS.find(n=>n.id===mission.sector); return <article className="group flex min-h-96 flex-col rounded-3xl border border-[#d7cebe] bg-[#fffaf4] p-6 transition hover:-translate-y-1 hover:border-[#d10e63]/40"><div className="flex items-center justify-between"><span className="font-mono text-[10px] font-black uppercase tracking-[.14em] text-[#d10e63]">{sector?.name}</span>{mission.free&&<span className="rounded-full bg-[#d10e63]/10 px-3 py-1 text-[10px] font-black uppercase text-[#b00b52]">Gratuit</span>}</div><h2 className="mt-7 text-2xl font-black tracking-[-.04em]">{mission.title}</h2><p className="mt-4 text-sm leading-7 text-[#625b50]">{mission.result}</p><div className="mt-6 grid gap-2 text-xs font-bold text-[#625b50]"><p className="flex gap-2"><Clock3 className="size-4 text-[#d10e63]"/>{mission.duration} · {mission.level}</p><p className="flex gap-2"><FileCheck2 className="size-4 text-[#d10e63]"/>{mission.deliverable}</p></div><Link href={`/academy/missions/${mission.slug}`} className="mt-auto inline-flex items-center gap-2 pt-8 text-sm font-black text-[#b00b52]">Voir la mission<ArrowRight className="size-4 transition group-hover:translate-x-1"/></Link></article> }
export function SkillCard({ skill }: { skill:LearningSkill }) { return <article className="flex min-h-80 flex-col rounded-3xl border border-[#d7cebe] bg-[#fffaf4] p-6"><p className="font-mono text-[10px] font-black uppercase tracking-[.14em] text-[#d10e63]">Compétence · v{skill.version}</p><h2 className="mt-7 text-2xl font-black tracking-[-.04em]">{skill.title}</h2><p className="mt-4 text-sm leading-7 text-[#625b50]">{skill.outcome}</p><p className="mt-6 flex gap-2 text-xs font-bold"><FileCheck2 className="size-4 text-[#d10e63]"/>{skill.evidence}</p><Link href={`/academy/competences/${skill.slug}`} className="mt-auto inline-flex items-center gap-2 pt-8 text-sm font-black text-[#b00b52]">Voir la compétence<ArrowRight className="size-4"/></Link></article> }
export function PathCard({ path }: { path:LearningPath }) { return <article className="flex min-h-80 flex-col rounded-3xl bg-[#181512] p-7 text-[#f8f1e7]"><Layers3 className="size-5 text-[#f2a4c5]"/><h2 className="mt-8 text-3xl font-black tracking-[-.05em]">{path.title}</h2><p className="mt-4 text-sm leading-7 text-[#cfc6b8]">{path.promise}</p><p className="mt-6 text-xs font-bold text-[#f2a4c5]">{path.format}</p><Link href={`/academy/parcours/${path.slug}`} className="mt-auto inline-flex items-center gap-2 pt-8 text-sm font-black text-[#f2a4c5]">Explorer le parcours<ArrowRight className="size-4"/></Link></article> }
