import Link from 'next/link'
import { ArrowRight } from 'lucide-react'
import { NETWORKS, type LearningMission, type LearningPath, type LearningSkill } from '@/lib/academy-catalog'

export function MissionCard({ mission }: { mission:LearningMission }) { const sector=NETWORKS.find(n=>n.id===mission.sector); return <article className="academy-list-item group"><p className="academy-kicker">{sector?.name}{mission.free?' · Gratuit':''}</p><h2 className="mt-3 text-2xl font-semibold tracking-[-.035em]">{mission.title}</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-[#625b50]">{mission.result}</p><p className="mt-4 text-xs font-semibold text-[#857c6e]">{mission.duration} · {mission.level} · {mission.deliverable}</p><Link href={`/academy/missions/${mission.slug}`} className="academy-text-link mt-4">Voir la mission<ArrowRight className="size-3.5 transition group-hover:translate-x-1"/></Link></article> }
export function SkillCard({ skill }: { skill:LearningSkill }) { return <article className="academy-list-item"><p className="academy-kicker">Compétence · v{skill.version}</p><h2 className="mt-3 text-2xl font-semibold tracking-[-.035em]">{skill.title}</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-[#625b50]">{skill.outcome}</p><p className="mt-4 text-xs font-semibold text-[#857c6e]">Preuve: {skill.evidence}</p><Link href={`/academy/competences/${skill.slug}`} className="academy-text-link mt-4">Voir la compétence<ArrowRight className="size-3.5"/></Link></article> }
export function PathCard({ path }: { path:LearningPath }) { return <article className="academy-list-item"><p className="academy-kicker">Parcours · {path.format}</p><h2 className="mt-3 text-2xl font-semibold tracking-[-.035em]">{path.title}</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-[#625b50]">{path.promise}</p><Link href={`/academy/parcours/${path.slug}`} className="academy-text-link mt-4">Explorer le parcours<ArrowRight className="size-3.5"/></Link></article> }
